server:
  port: 8082
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/managerDB
    username: 'postgres'
    password: 'chiheb2002'
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true
